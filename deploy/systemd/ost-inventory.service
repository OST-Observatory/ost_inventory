[Unit]
Description=OST Inventory gunicorn daemon
Requires=ost-inventory.socket
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/opt/ost_inventory
EnvironmentFile=/opt/ost_inventory/.env
Environment=DJANGO_ENV=production
Environment=PYTHONUNBUFFERED=1
SyslogIdentifier=ost-inventory
StandardOutput=journal
StandardError=journal
SyslogLevelPrefix=true
ExecStart=/opt/ost_inventory/.venv/bin/gunicorn \
    --workers 3 \
    --timeout 120 \
    --access-logfile - \
    --error-logfile - \
    --no-control-socket \
    --bind unix:/run/ost-inventory/gunicorn.sock \
    config.wsgi:application
Restart=on-failure
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=yes
PrivateTmp=yes
UMask=0077
MemoryMax=512M
ReadWritePaths=/opt/ost_inventory/media /run/ost-inventory

[Install]
WantedBy=multi-user.target
